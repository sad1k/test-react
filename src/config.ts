/**
 * Глобальные константы приложения.
 *
 * Оба холста (PixiJS и Skia) работают в одной логической системе координат
 * размером `width × height`. CSS растягивает их по ширине контейнера,
 * сохраняя пропорции, поэтому пиксель сцены на обоих движках совпадает.
 */
export const CONFIG = {
  /** Логическая ширина сцены (в «пикселях» сцены). */
  width: 760,
  /** Логическая высота сцены. */
  height: 520,
  /** URL демонстрационного PNG-спрайта (лежит в public/assets). */
  spriteUrl: `${import.meta.env.BASE_URL}assets/sample.png`,
  /** Имя выгружаемого PDF-файла. */
  pdfFileName: 'pixi-skia-scene.pdf',
} as const;
